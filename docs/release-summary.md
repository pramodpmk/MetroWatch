## v2 Release Summary

**Base:** `v1` → HEAD
**Generated:** 2026-06-11

### What shipped

- Added a Water Metro home tab with dedicated stations, routes, timings, and fares screens.
- Riders can now pick departure/arrival stations for trip planning directly from the home screen.
- Fixed train timing calculations so times are based on the line's actual terminus origin, not the rider's own station.
- Fixed a bug where location detection could fail on a freshly installed Android app.
- Redesigned home, station list, station picker, plan trip, fare calculator, timing, metro routes, and parking screens, with initial dark-mode support.
- Rebranded the app: new package identity (`com.metrowatch.kochi`), new name, launcher icon, and splash logo.
- Station taps now navigate to that station's specific detail screen instead of a generic one.
- Refactored the trip/fare/timing repository layer so business logic lives in use cases, not repository implementations.
- Reset the local Room database to a clean version 1 baseline under the new package/app name.

### Key numbers

- 4 new features (Water Metro home section, home-screen trip station picker, Water Metro fares screen, richer fare details)
- 3 bug fixes (train timing calculation, Android location fresh-install, malformed Gradle include)
- 0 performance improvements
- 0 security improvements
- 2 dependencies added (`desugar_jdk_libs`, Compose extended Material Icons)

### Files in this release

- `docs/sprints/v2.md` — Full business sprint summary
- `docs/technical/v2-technical.md` — Engineering technical notes
- `docs/CHANGELOG.md` — Cumulative changelog entry

### Notes for reviewers

- `Route.StationDetail` is now a parameterized route requiring a `stationId` — confirm all call sites were updated.
- The Room database version was reset to 1 and renamed to `metro_watch.db`; there's no migration path yet for the next schema change.
- `gradle.properties` now hardcodes a local JDK path (`org.gradle.java.home`) — this should likely be removed before other developers or CI build from this branch.
- The Water Metro general fares list is currently hardcoded in `FareViewModel`, not sourced from the synced config data.
- QA should specifically re-test timing/trip results at both terminus and interior stations in both directions, and confirm location detection on a genuinely fresh Android install.

---
*Generated by release-doc-generator from `git diff v1..HEAD`*
